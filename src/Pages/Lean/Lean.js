import React from 'react';
import LeeanTransformation from '../../Component/LeeanTransformation/LeeanTransformation';

const Lean = () => {
    return (
        <div>
            <LeeanTransformation/>
        </div>
    );
};

export default Lean;